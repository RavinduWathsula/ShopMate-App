import 'package:flutter/material.dart';

class ShoppingSummaryScreen extends StatelessWidget {
  const ShoppingSummaryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ShoppingSummaryScreen')),
      body: const Center(child: Text('ShoppingSummaryScreen Placeholder')),
    );
  }
}

