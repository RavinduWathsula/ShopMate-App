import 'package:flutter/material.dart';

class SupermarketSelectionScreen extends StatelessWidget {
  const SupermarketSelectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SupermarketSelectionScreen')),
      body: const Center(child: Text('SupermarketSelectionScreen Placeholder')),
    );
  }
}
