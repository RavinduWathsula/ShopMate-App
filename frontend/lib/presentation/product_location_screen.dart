import 'package:flutter/material.dart';

class ProductLocationScreen extends StatelessWidget {
  const ProductLocationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ProductLocationScreen')),
      body: const Center(child: Text('ProductLocationScreen Placeholder')),
    );
  }
}
